Put the zmq_pose_server folder and CMakeLists.txt inside the openpose/examples folder and compile openpose as explained on openpose github
Obviously you'll need zmq installed.
Then you can run the server by running from openpose folder
./build/examples/zmq_pose_server/multipleimages.bin 
